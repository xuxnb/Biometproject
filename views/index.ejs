<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>项目管理系统</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">项目管理系统</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">项目列表</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/projects/new">新建项目</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4">
        <div class="row">
            <div class="col-md-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1>项目列表</h1>
                    <a href="/projects/new" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> 新建项目
                    </a>
                </div>
                
                <% if (projects && projects.length) { %>
                    <div class="row project-list">
                        <% projects.forEach(project => { %>
                            <div class="col-md-4 mb-4">
                                <a href="/projects/<%= project.id %>" class="text-decoration-none">
                                    <div class="card h-100">
                                        <div class="card-body">
                                            <h5 class="card-title"><%= project.name %></h5>
                                            <p class="card-text text-muted">
                                                <%= project.description ? project.description.substring(0, 100) + (project.description.length > 100 ? '...' : '') : '无描述' %>
                                            </p>
                                            <div class="d-flex justify-content-between align-items-center">
                                                <% 
                                                let statusClass = '';
                                                switch(project.status) {
                                                    case '进行中':
                                                        statusClass = 'status-active';
                                                        break;
                                                    case '已完成':
                                                        statusClass = 'status-completed';
                                                        break;
                                                    case '已延期':
                                                        statusClass = 'status-delayed';
                                                        break;
                                                    case '规划中':
                                                        statusClass = 'status-planned';
                                                        break;
                                                    default:
                                                        statusClass = 'status-active';
                                                }
                                                %>
                                                <span class="project-status <%= statusClass %>"><%= project.status || '进行中' %></span>
                                                <small class="text-muted">
                                                    开始: <%= project.start_date || '未设置' %>
                                                </small>
                                            </div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="alert alert-info" role="alert">
                        暂无项目，请点击"新建项目"按钮创建一个新项目。
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="bg-light py-3 mt-5">
        <div class="container text-center">
            <p class="text-muted mb-0">&copy; <%= new Date().getFullYear() %> 项目管理系统</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html> 